
#include <string>
#include <array>
#include <random>
#include <sstream>

#pragma once

namespace tinychain {

namespace tc = tinychain;

using sha256_t = std::string;
using md5_t = std::string;
using address_t = std::string;
using script_sign_t = std::string;
using script_pubkey_t = std::string;
using data_t = std::string;

}

